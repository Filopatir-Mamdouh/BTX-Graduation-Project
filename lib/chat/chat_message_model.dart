import 'package:graduation_project/chat/chat_message.dart';

class ChatMessageModel extends ChatMessage {
  const ChatMessageModel(
      {required super.email,
      required super.name,
      required super.role,
      required super.message,
      required super.time});
}
